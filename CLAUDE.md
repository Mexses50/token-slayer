# CLAUDE.md

> Auto-generated by [claude-context-analyzer](https://github.com/yourusername/claude-context-analyzer).
> Review and edit before committing.

## How to Work on This Project

**Always follow these steps — do NOT read source files directly without checking first:**

1. **Start of session** → call `snapshot_tool` with the project path to get the full overview
2. **Before reading any file** → call `focus_tool` with the project path + what you're working on
3. **Only read the files** that `focus_tool` returns — nothing else
4. **After making changes** → run `tslayer audit .` to verify health

These MCP tools are available automatically. Using them reduces context by 80-90%.

## Project Overview

- **Files:** 60 Python files
- **Lines of code:** 5,270
- **Functions:** 409  |  **Classes:** 56
- **Type coverage:** 34%  |  **Avg complexity:** 6.7
- **Estimated token reduction with .claudeignore:** 53.0%
- **Detected frameworks:** Rich, Typer, pytest

## Project Structure

```
src/
  cca/
    lang/
test-project/
  app/
    api/
    db/
    models/
    services/
    utils/
tests/
```

## Hot Files (Change Frequently)

Based on recent git history:

| File | Changes |
|------|---------|
| `src/cca/cli.py` | 6 |
| `src/cca/mcp_server.py` | 3 |
| `src/cca/dead_code.py` | 2 |
| `src/cca/health.py` | 2 |
| `src/cca/snapshot.py` | 2 |
| `src/cca/focus.py` | 2 |

## Possible Dead Code

> Defined but never imported elsewhere. Verify before removing - 
> dynamic access or `__all__` usage may not be detected.

- **`src/cca/cache.py`**: `invalidate`

## Recommended .claudeignore

```
__pycache__/
*.pyc
.venv/
venv/
.git/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
*.log
*.lock
```
